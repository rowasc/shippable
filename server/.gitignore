node_modules/
.env*
*.log

# Bun-compiled sidecar binaries (built by `bun build --compile`,
# bundled into the Tauri app at build time, never committed).
dist/

# Bun crash dumps.
bun-debug-*.log

# Bun --compile temp staging files left behind by interrupted builds.
.*.bun-build
