# SDD Configuration

root: docs/sdd

SDD artifacts (requirements, specs, plans, implementation notes, roadmaps) live under `docs/sdd/` rather than the default `sdd/`. This keeps spec-driven design artifacts colocated with the rest of the project documentation under `docs/`.
