# Prompt Results

## What it is
The stacked output area for prompt runs.

## What it does
- Streams prompt output as it arrives.
- Keeps multiple prompt runs visible at once instead of replacing the last result.
- Shows run state clearly with `streaming`, `done`, and `error`.
- Lets the reviewer dismiss runs individually.

## Screenshot
![Prompt results](./assets/prompt-results.png)
